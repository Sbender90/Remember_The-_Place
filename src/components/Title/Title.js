import React from "react";
import "./Title.css";

const Title = () => (
    <div id="title">
        <div id="title_content">Don't travel to the same location twice!</div>
    </div>
);

export default Title;


// {/* <h1 className="title">{props.children}</h1> */}