import React from "react";
import "./Navbar.css";


const Navbar = props => (
  <div id="navBar">
    <div id="navBar_content">Remember the pictures, click on pictures to get points, don't click the same picture twice or it's game over.</div>
  </div>
);

export default Navbar;  
  

  